[project]
name = "youtube-transcribed"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "requests[socks]>=2.33.1",
    "uvicorn[standard]>=0.32",
    "youtube-transcript-api>=1.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8",
    "httpx>=0.27",
]
